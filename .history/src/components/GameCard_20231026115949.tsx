import React from 'react'
import { Game } from '../hooks/useGames'

interface Props {
    game: Game
}

const GameCard = ({games}:) => {
  return (
    <div>GameCard</div>
  )
}

export default GameCard